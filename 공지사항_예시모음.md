# 📝 **공지사항 예시 모음집**

> **복사해서 바로 쓸 수 있는 공지사항 템플릿**

---

## 🎉 **좋은 소식 / 새 기능 (초록색)**

### **신기능 출시**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"🚀 신기능 출시!\", \"body\": \"**Engine A/B 비교** 기능이 새로 추가되었습니다!\\n\\n이제 두 개의 AI 모델 응답을 동시에 비교할 수 있어요.\", \"severity\": \"success\", \"pinned\": true, \"lang\": \"ko\"}"
```

### **베타 오픈**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"🎉 TOCMOOD 베타 오픈!\", \"body\": \"감정관리의 **새로운 시작**\\n\\n✅ AI 개인화 상담\\n✅ AR EFT 탭핑 가이드\\n✅ 게임형 통찰 시스템\", \"severity\": \"success\", \"pinned\": true, \"lang\": \"ko\"}"
```

### **이벤트 시작**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"🎁 특별 이벤트 시작!\", \"body\": \"**일주일간 특별 이벤트**\\n\\n매일 AI 상담을 받으면 특별 뱃지를 드려요! 🏆\", \"severity\": \"success\", \"pinned\": true, \"lang\": \"ko\"}"
```

### **품질 개선**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"⬆️ AI 품질 대폭 개선!\", \"body\": \"**AI 상담 품질이 30% 향상**되었습니다!\\n\\n더 자연스럽고 도움이 되는 대화를 경험해보세요.\", \"severity\": \"success\", \"pinned\": false, \"lang\": \"ko\"}"
```

---

## ⚠️ **주의사항 / 점검 안내 (노란색)**

### **정기 점검**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"⚠️ 정기 점검 안내\", \"body\": \"**오늘 밤 23:00-23:30 서버 점검**\\n\\n더 나은 서비스를 위한 정기 점검입니다.\\n점검 시간 중에는 일시적으로 접속이 어려울 수 있습니다.\", \"severity\": \"warning\", \"pinned\": true, \"lang\": \"ko\"}"
```

### **임시 서비스 제한**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"⚠️ 일시적 서비스 제한\", \"body\": \"**AI 상담 응답이 평소보다 느릴 수 있습니다**\\n\\n서버 부하로 인한 일시적 현상이며, 곧 정상화될 예정입니다.\", \"severity\": \"warning\", \"pinned\": true, \"lang\": \"ko\"}"
```

### **업데이트 예정**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"⚠️ 업데이트 예정\", \"body\": \"**내일 오전 새로운 업데이트**가 적용됩니다\\n\\n일부 기능이 변경될 수 있으니 미리 확인해주세요.\", \"severity\": \"warning\", \"pinned\": false, \"lang\": \"ko\"}"
```

### **사용량 증가 안내**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"⚠️ 사용량 급증 안내\", \"body\": \"**현재 사용량이 평소보다 많습니다**\\n\\n응답 속도가 느려질 수 있으니 양해 부탁드립니다. 🙏\", \"severity\": \"warning\", \"pinned\": true, \"lang\": \"ko\"}"
```

---

## 🚨 **긴급 / 중요 (빨간색)**

### **서비스 장애**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"🚨 긴급 공지\", \"body\": \"**일부 기능에 오류가 발생**했습니다\\n\\n현재 복구 작업 중이며, 빠른 시일 내에 정상화하겠습니다.\", \"severity\": \"critical\", \"pinned\": true, \"lang\": \"ko\"}"
```

### **보안 업데이트**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"🚨 중요 보안 업데이트\", \"body\": \"**보안 강화를 위한 긴급 업데이트**\\n\\n모든 사용자는 앱을 다시 시작해주세요.\", \"severity\": \"critical\", \"pinned\": true, \"lang\": \"ko\"}"
```

### **데이터 복구**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"🚨 데이터 복구 완료\", \"body\": \"**일시적 데이터 손실이 복구**되었습니다\\n\\n모든 기능이 정상 작동합니다. 불편을 드려 죄송합니다.\", \"severity\": \"critical\", \"pinned\": true, \"lang\": \"ko\"}"
```

---

## 💡 **일반 정보 / 팁 (파란색)**

### **사용법 안내**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"💡 EFT 탭핑 팁\", \"body\": \"**효과적인 EFT 탭핑 방법**\\n\\n✅ 천천히, 일정한 리듬으로\\n✅ 감정에 집중하면서\\n✅ 깊게 숨쉬며 진행하세요\", \"severity\": \"info\", \"pinned\": false, \"lang\": \"ko\"}"
```

### **앱 사용 가이드**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"💡 앱 활용 가이드\", \"body\": \"**TOCMOOD를 더 효과적으로 사용하는 법**\\n\\n1. 매일 감정 체크인\\n2. AI 상담으로 마음 나누기\\n3. EFT 탭핑으로 감정 정리\", \"severity\": \"info\", \"pinned\": false, \"lang\": \"ko\"}"
```

### **건강한 습관**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"💡 오늘의 마음 관리 팁\", \"body\": \"**하루 5분 마음챙김**\\n\\n잠깐 멈춰서 깊게 숨쉬고\\n지금 이 순간의 감정을 느껴보세요 🌿\", \"severity\": \"info\", \"pinned\": false, \"lang\": \"ko\"}"
```

### **통계 공유**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"📊 이달의 통계\", \"body\": \"**TOCMOOD 사용자들의 긍정적 변화**\\n\\n😊 스트레스 감소: 평균 30%\\n🌟 기분 개선: 평균 40%\\n💪 감정 조절력 향상: 평균 25%\", \"severity\": \"info\", \"pinned\": false, \"lang\": \"ko\"}"
```

---

## 🌍 **영어 공지사항**

### **Welcome Message**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"🌟 Welcome to TOCMOOD!\", \"body\": \"**Your emotional wellness journey starts here**\\n\\nExperience AI counseling and EFT tapping in a gamified way.\", \"severity\": \"success\", \"pinned\": true, \"lang\": \"en\"}"
```

### **New Feature**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"🚀 New Feature Released!\", \"body\": \"**Engine A/B Comparison** is now available!\\n\\nCompare two AI models side by side for better responses.\", \"severity\": \"success\", \"pinned\": true, \"lang\": \"en\"}"
```

### **Maintenance**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"⚠️ Scheduled Maintenance\", \"body\": \"**Server maintenance tonight 11PM-11:30PM**\\n\\nService may be temporarily unavailable during this time.\", \"severity\": \"warning\", \"pinned\": true, \"lang\": \"en\"}"
```

---

## 📅 **시간 제한 공지사항**

### **일주일간 이벤트**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"🎁 7일간 특별 이벤트\", \"body\": \"**한 주간 특별 혜택**\\n\\n매일 로그인하면 보너스 젬을 드려요! 💎\", \"severity\": \"success\", \"pinned\": true, \"startsAt\": \"2025-09-23T00:00:00Z\", \"endsAt\": \"2025-09-30T23:59:59Z\", \"lang\": \"ko\"}"
```

### **점검 시간 안내**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"⚠️ 내일 점검 안내\", \"body\": \"**2025년 9월 25일 새벽 2시-3시**\\n서버 점검이 진행됩니다.\", \"severity\": \"warning\", \"pinned\": true, \"startsAt\": \"2025-09-24T00:00:00Z\", \"endsAt\": \"2025-09-25T15:00:00Z\", \"lang\": \"ko\"}"
```

---

## 🎨 **특별한 상황별 공지사항**

### **첫 사용자 환영**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"🎉 첫 방문을 환영합니다!\", \"body\": \"**TOCMOOD에 오신 것을 환영해요!**\\n\\n🌟 AI 상담으로 마음을 나누고\\n✨ EFT 탭핑으로 감정을 정리하고\\n🎮 게임하듯 즐겁게 성장하세요!\", \"severity\": \"success\", \"pinned\": true, \"lang\": \"ko\"}"
```

### **피드백 요청**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"💌 소중한 의견을 들려주세요\", \"body\": \"**더 나은 TOCMOOD를 만들어가요**\\n\\n여러분의 경험과 제안이 큰 도움이 됩니다.\\n언제든 편하게 의견을 나눠주세요! 🙏\", \"severity\": \"info\", \"pinned\": false, \"lang\": \"ko\"}"
```

### **감사 인사**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"🙏 감사합니다\", \"body\": \"**TOCMOOD를 사랑해주시는 모든 분들께**\\n\\n여러분 덕분에 더 좋은 서비스를 만들어 갈 수 있습니다.\\n앞으로도 함께 성장해요! ❤️\", \"severity\": \"success\", \"pinned\": false, \"lang\": \"ko\"}"
```

### **연휴 안내**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"🏖️ 연휴 기간 안내\", \"body\": \"**추석 연휴 동안**\\n\\n모든 서비스는 정상 운영됩니다.\\n가족과 함께 행복한 시간 보내세요! 🌕\", \"severity\": \"info\", \"pinned\": false, \"lang\": \"ko\"}"
```

---

## 🔄 **시리즈 공지사항**

### **일일 팁 시리즈**
```bash
# 월요일
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"💡 월요일 마음 관리 팁\", \"body\": \"**새로운 한 주를 시작하며**\\n\\n오늘은 '감사한 일 3가지'를 떠올려보세요.\\n긍정적인 에너지로 한 주를 시작해요! ✨\", \"severity\": \"info\", \"pinned\": false, \"lang\": \"ko\"}"

# 화요일
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"💡 화요일 마음 관리 팁\", \"body\": \"**화요병을 이겨내는 방법**\\n\\n5분간 좋아하는 음악을 들으며\\n가볍게 몸을 움직여보세요 🎵\", \"severity\": \"info\", \"pinned\": false, \"lang\": \"ko\"}"
```

### **주간 도전 과제**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"🎯 이번 주 도전과제\", \"body\": \"**주간 마음 챙김 챌린지**\\n\\n매일 10분씩 EFT 탭핑하기\\n한 주 완주하면 특별 뱃지를 드려요! 🏆\", \"severity\": \"success\", \"pinned\": true, \"lang\": \"ko\"}"
```

---

## 📱 **사용 팁**

### **제목 작성 팁**
- 🎨 **이모지 활용**: 한눈에 알아보기 쉽게
- 📝 **간결하게**: 20자 이내 권장
- ⚡ **긴급도 표시**: 🚨(긴급), ⚠️(주의), 💡(정보), 🎉(기쁨)

### **본문 작성 팁**
- **굵게**: `**중요한 내용**`
- **줄바꿈**: `\\n` 사용
- **목록**: `✅`, `•`, `1.` 등 활용
- **이모지**: 감정 전달에 효과적

### **색상 가이드**
- 🔵 **info**: 일반 정보, 팁, 가이드
- 🟢 **success**: 좋은 소식, 신기능, 이벤트
- 🟡 **warning**: 주의사항, 점검, 변경사항
- 🔴 **critical**: 긴급상황, 오류, 보안

**💡 이 예시들을 참고해서 여러분만의 공지사항을 만들어보세요!**