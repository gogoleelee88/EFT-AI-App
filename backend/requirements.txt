# Render fallback requirements
# If build runs at repo root, include backend requirements:
-r backend/requirements.txt
vLLM이# EFT AI 서버 의존성 패키지

# FastAPI 서버
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0

# AI/ML 관련 (OpenAI SDK 방식으로 변경)
openai>=1.0.0
# transformers==4.36.0  # vLLM 사용으로 불필요
# torch>=2.6.0  # vLLM 사용으로 불필요
# accelerate==0.25.0  # vLLM 사용으로 불필요
# bitsandbytes==0.41.3  # vLLM 사용으로 불필요
# sentencepiece==0.1.99  # vLLM 사용으로 불필요

# GPU 모니터링 (vLLM이 처리)
# GPUtil==1.4.0  # vLLM 사용으로 불필요

# 추론 최적화 (선택사항)
# vllm==0.2.6  # GPU 추론 가속 (Windows 호환성 이슈로 주석)
# ollama-python==0.1.7  # 로컬 LLM 관리

# 데이터베이스
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
# sqlite3  # Python 내장 라이브러리 (pip 설치 불필요)
redis==5.0.1  # 캐싱용 (선택사항)

# Auth (Firebase Admin)
firebase-admin==6.5.0
pywebpush==2.0.3

# 유틸리티
python-dotenv==1.0.0
httpx==0.27.2
cryptography==41.0.7
numpy==1.24.3
pandas==2.1.4
Pillow>=10.0.0

# Google Calendar / OAuth (Phase 2)
google-auth==2.28.2
google-auth-oauthlib==1.2.0
google-api-python-client==2.111.0

# 로깅 및 모니터링
loguru==0.7.2
prometheus-client==0.19.0

# CORS 및 보안
python-jose[cryptography]==3.3.0
python-multipart==0.0.6
passlib[bcrypt]==1.7.4

# 개발용
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
supabase==2.8.1

gotrue==2.8.1