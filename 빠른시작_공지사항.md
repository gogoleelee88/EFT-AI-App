# ⚡ **빠른 시작 - 공지사항 3분 완성**

> **초보자도 3분 안에 첫 공지사항 만들기**

---

## 🚀 **1단계: 서버 켜기 (30초)**

1. **폴더 열기**: `moodtalk_public\backend` 폴더로 이동
2. **명령창 열기**: `Shift + 우클릭` → "PowerShell 창 여기서 열기"
3. **서버 실행**: `python main.py` 입력 후 엔터

✅ **성공 신호**: "서버가 시작되었습니다" 메시지 확인

---

## ⚡ **2단계: 첫 공지 만들기 (1분)**

**아래 명령어를 복사해서 붙여넣으세요:**

```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"🎉 TOCMOOD 오픈!\", \"body\": \"**감정관리의 새로운 시작**입니다!\", \"severity\": \"success\", \"pinned\": true, \"lang\": \"ko\"}"
```

✅ **성공 신호**: JSON 형태의 긴 텍스트가 출력되면 성공!

---

## 👀 **3단계: 확인하기 (1분)**

### **방법 1: 앱에서 확인**
1. 브라우저 열기
2. 주소창에 `http://localhost:5173` 입력
3. 앱 맨 위 초록색 배너 확인! 🎉

### **방법 2: API로 확인**
```bash
curl http://localhost:8000/api/notices
```

---

## 🎨 **보너스: 다른 색깔 공지 만들기**

### **⚠️ 주의 공지 (노란색)**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"⚠️ 점검 안내\", \"body\": \"오늘 밤 **점검**이 있습니다\", \"severity\": \"warning\", \"lang\": \"ko\"}"
```

### **🚨 긴급 공지 (빨간색)**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"🚨 긴급\", \"body\": \"중요한 **업데이트**가 있습니다\", \"severity\": \"critical\", \"lang\": \"ko\"}"
```

### **💡 일반 공지 (파란색)**
```bash
curl -X POST http://localhost:8000/api/notices -H "Content-Type: application/json" -H "X-API-Key: test-admin-key-12345" -d "{\"title\": \"💡 팁\", \"body\": \"**EFT 탭핑**을 천천히 해보세요\", \"severity\": \"info\", \"lang\": \"ko\"}"
```

---

## 🗑️ **공지 삭제하기**

1. **ID 찾기**: `curl http://localhost:8000/api/notices`
2. **삭제하기**: `curl -X DELETE http://localhost:8000/api/notices/[ID] -H "X-API-Key: test-admin-key-12345"`

---

## 🆘 **문제 해결**

| 문제 | 해결법 |
|------|--------|
| "curl을 찾을 수 없습니다" | Windows라면 PowerShell 사용 권장 |
| "연결할 수 없습니다" | `python main.py`로 서버 재시작 |
| "권한이 없습니다" | API 키 `test-admin-key-12345` 확인 |

---

## 🎯 **다음 단계**

- 📖 **자세한 매뉴얼**: `NOTICE_MANUAL.md` 파일 읽기
- 🎨 **고급 기능**: 시간 설정, 마크다운 사용법 등
- 🔧 **개발자 도구**: `http://localhost:8000/docs` 방문

**🎉 축하합니다! 공지사항 관리자가 되셨습니다!**